const articleList=document.getElementById('articleList');const searchInput=document.getElementById('searchInput');const searchButton=document.getElementById('searchButton');let articles=[];function fetchIssues(){fetch('https://api.github.com/repos/ReplicantHunter/QUANTUM-VAULT/issues?state=open').then(response=>{if(!response.ok){throw new Error('Network response was not ok: '+response.statusText)}
return response.json()}).then(issues=>{articleList.innerHTML='';articles=issues;if(issues.length===0){const noArticlesMessage=document.createElement('p');noArticlesMessage.textContent='No articles available at the moment.';articleList.appendChild(noArticlesMessage)}else{displayArticles(issues)}}).catch(error=>{console.error('Error fetching issues:',error);const errorMessage=document.createElement('p');errorMessage.textContent='Failed to load articles. Please try again later.';articleList.appendChild(errorMessage)})}
function displayArticles(articlesToDisplay){articleList.innerHTML='';articlesToDisplay.forEach(issue=>{const articleDiv=document.createElement('div');articleDiv.classList.add('article');const articleLink=document.createElement('a');articleLink.href=`article.html?id=${issue.number}`;articleLink.textContent=issue.title;articleDiv.appendChild(articleLink);articleList.appendChild(articleDiv)})}
function searchArticles(){const query=searchInput.value.toLowerCase();const filteredArticles=articles.filter(article=>article.title.toLowerCase().includes(query));if(filteredArticles.length===0){articleList.innerHTML='<p>No articles match your search.</p>'}else{displayArticles(filteredArticles)}}
searchButton.addEventListener('click',searchArticles);searchInput.addEventListener('keydown',function(event){if(event.key==='Enter'){searchArticles()}});if(articleList){window.onload=fetchIssues}
